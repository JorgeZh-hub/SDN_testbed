drive:
  method: rclone
  remote: "UIC_2025:"

paths:
  raw_root: "PruebaX_final/raw"
  extracted_root: "PruebaX_final/extracted"

defaults:
  zip_glob: "*.zip"
  rclone_flags:
    - "--checksum"
    - "--progress"

scenarios:
  - name: "Baseline"
    drive_paths:
      - "UIC_backup/capturas_finales/top1_Baseline" # dentro de Drive
      - "UIC_backup/capturas_finales/top1_Baseline_p2"
