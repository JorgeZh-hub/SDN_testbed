drive:
  method: rclone
  remote: "UIC_2025:"

paths:
  raw_root: "data_top2_1/raw"
  extracted_root: "data_top2_1/extracted"

defaults:
  zip_glob: "*.zip"
  rclone_flags:
    - "--checksum"
    - "--progress"

scenarios:
  - name: "Baseline"
    drive_path: "UIC_backup/captures_top2/test2_Baseline" # dentro de Drive
