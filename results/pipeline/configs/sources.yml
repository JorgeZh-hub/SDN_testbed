drive:
  method: rclone
  remote: "UIC_2025:"

paths:
  raw_root: "Prueba1_final/raw"
  extracted_root: "Prueba1_final/extracted"

defaults:
  zip_glob: "*.zip"
  rclone_flags:
    - "--checksum"
    - "--progress"

scenarios:
  - name: "Baseline"
    drive_path: "UIC_backup/capturas_finales/top1_Baseline" # dentro de Drive
